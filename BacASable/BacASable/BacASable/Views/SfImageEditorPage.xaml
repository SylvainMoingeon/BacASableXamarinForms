<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BacASable.ViewModels"
             xmlns:imageeditor="clr-namespace:Syncfusion.SfImageEditor.XForms;assembly=Syncfusion.SfImageEditor.XForms"
             xmlns:converters="clr-namespace:BacASable.Converters"
             x:Class="BacASable.Views.SfImageEditorPage">

    <ContentPage.Resources>
        <converters:DebugConverter x:Key="DebugConverter" />
    </ContentPage.Resources>
    
    <ContentPage.BindingContext>
        <vm:SfImageEditorViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <imageeditor:SfImageEditor Grid.Row="0"  Source="{Binding Image, Converter={StaticResource DebugConverter}}" x:Name="imageeditor" />
            <Button Grid.Row="1" Text="Pick Photo" Command="{Binding PickPhotoCommand}"></Button>
        </Grid>
    </ContentPage.Content>


</ContentPage>